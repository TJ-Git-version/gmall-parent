nacos:
  server-addr: 192.168.136.132:8848
spring:
  application:
    name: service-list
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: ${nacos.server-addr}
      config:
        server-addr: ${nacos.server-addr}
        file-extension: yaml
        prefix: ${spring.application.name}
        shared-configs:
          - data-id: common.yaml
            refresh: true
