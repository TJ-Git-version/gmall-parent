<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper SYSTEM "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.atguigu.gmall.product.mapper.SpuImageMapper">

    <insert id="insertBatch">
        insert into spu_image (spu_id, img_name, img_url, create_time, update_time, is_deleted)
        values
        <foreach collection="spuImageList" item="spuImage" separator=",">
            (#{spuImage.spuId}, #{spuImage.imgName}, #{spuImage.imgUrl}, now(), now(), 0)
        </foreach>
    </insert>
</mapper>