spring:
  application:
    name: service-item
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.37.132:8848
      config:
        server-addr: 192.168.37.132:8848
        prefix: ${spring.application.name}
        file-extension: yaml
        shared-configs: # 共享配置
          - data-id: common.yaml
            refresh: true # 共享配置是否刷新
